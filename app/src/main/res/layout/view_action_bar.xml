<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/viewActionBar"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize">

    <ImageButton android:id="@+id/btnProfile"
        android:layout_gravity="center"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="50dp"
        android:src="@drawable/ic_search_white_24dp"
        android:tint="@color/colorGrayPrimary"
        android:background="@android:color/transparent"/>

    <TextView android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_marginRight="5dp"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="@dimen/app_title_text_size"
        android:textColor="@color/colorPrimary"/>

    <ImageButton android:id="@+id/btnProfile2"
        android:layout_gravity="center"
        android:layout_marginRight="60dp"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/ic_person_outline_primary_24dp"
        android:background="@drawable/profile_button_shape"
        android:backgroundTint="@color/colorPrimary" />

</LinearLayout>